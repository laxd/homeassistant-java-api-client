package uk.laxd.homeassistantclient.model.json

/**
 * Represents a response from the Home Assistant server, either to a listener/trigger that
 * we set up (Event or Trigger), or to a message that we sent.
 */
class Response {

}
